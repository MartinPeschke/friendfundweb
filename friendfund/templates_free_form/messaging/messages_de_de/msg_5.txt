Hallo ${data['admin_name']},  

Herzlichen Glückwunsch! Die Gründung deines Pools war erfolgreich:   
${data['pool_title']} für ${data['pool_description']}.

Einladungen wurden geschickt an ${self.show_names(data['invitee_list'])}  

[Wirb für deinen Pool](http://${data['merchant_domain']}/pool/${data['p_url']}) oder [lade mehr Freunde ein](http://${data['merchant_domain']}/pool/${data['p_url']}), um den Gesamtbetrag von ${data['target_amount']} bis  ${data['expiry_date']} zu erreichen.  


Alles Gute,  
%if data['merchant_is_default']:      
das Team @friendfund
%else:  
das Team @${data['merchant_name']} in Kooperation mit friendfund   
<img src="${data['merchant_logo_url']}" alt="${data['merchant_name']}"/>   
%endif 

<${data['DEFAULT_BASE_URL']}>  
Sammle Geld mit deinen Freunden. Gründe einen Pool. Mach mit. friendfund it!

<%def name="subject()">Pool erstellt auf friendfund</%def>
<%def name="show_names(invitees)">${", ".join(ivt["name"] for ivt in invitees)}</%def>
