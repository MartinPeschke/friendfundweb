Hallo ${data['firstname_invitee_name']},  

${data['commenter_name']} hat folgenden ${data['pool_title']} Komentar hinzugefügt:

${data['invitee_comment']}  

Es sind noch ${data['x_days_left']} Tage übrig, um den Gesamtbetrag in von ${data['target_amount']} für  ${data['pool_description']} zu sammeln.

<http://${data['merchant_domain']}/pool/${data['p_url']}>  

[Wirb für diesen Pool](http://${data['merchant_domain']}/pool/${data['p_url']}) oder [lade weitere Freunde ein](http://${data['merchant_domain']}/pool/${data['p_url']}) mitzumachen.     

Alles Gute,  
%if data['merchant_is_default']:      
das Team @friendfund
%else:  
das Team @${data['merchant_name']} in Kooperation mit friendfund   
<img src="${data['merchant_logo_url']}" alt="${data['merchant_name']}"/>   
%endif 

<${data['DEFAULT_BASE_URL']}>  
Sammle Geld mit deinen Freunden. Gründe einen Pool. Mach mit. friendfund it!

Hier kannst du friendfund Nachrichten abbestellen: <http://${data['DEFAULT_BASE_URL']}/myprofile/notifications>    

<%def name="subject()">${data['invitee_comment']} hat ${data['pool_title']} kommentiert</%def>   