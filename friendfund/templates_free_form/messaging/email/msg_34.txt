Hi ${data['invitee_name']} ${data['admin_name']},  

${data['invitee_comment_name']} has left a comment for ${data['pool_title']}!   

${data['invitee_comment']}  

There are still ${data['x_days_left']} days left to help out ${data['pool_description']} reach it’s target of ${data['target_amount']}.    

<http://${data['merchant_domain']}/pool/${data['p_url']}>  

[Promote this pool](http://${data['merchant_domain']}/pool/${data['p_url']}) or [invite some more friends] to join in (http://${data['merchant_domain']}/pool/${data['p_url']}).   

All the best   
the team @friendfund  

<DEFAULT_BASE_URL>  
The simple and fun way to collect money with your friends.  Create a pool and chip in!  

Don’t want to know? You can unsubscribe from messages from this pool here: <${data['unsubscribe']}>  

<%def name="subject()">${data['invitee_comment_name']} has left a comment for ${data['pool_title']}</%def>   