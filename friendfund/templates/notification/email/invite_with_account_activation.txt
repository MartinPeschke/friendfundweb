Hi ${data['name']},

${data['description']}

Open the link below in your browser to activate your account and join the funding effort.

${data['ROOT_URL']}/myprofile/tlogin/${data['activation_token']}?furl=/pool/${data['up_url']}

Best wishes,
${data['admin']}