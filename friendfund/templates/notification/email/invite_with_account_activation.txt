Hi ${data['name']},

${data['description']}

Open the link below in your browser to activate your account and join the funding effort.

${data['ROOT_URL']}/myprofile/setpassword/${data['activation_token']}

Best wishes,
${data['admin']}