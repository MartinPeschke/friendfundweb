Hi ${data['admin']},

Your pool for ${data['receiver']} is approaching the deadline and we still have to raise ${data['remaining_amount']} ${data['currency']}.

Come and vist the pool by opening the link below in your browser and execute your remaining fund raising actions. 

<${data['SITE_ROOT']}/pool/${data['p_url']}>

Best wishes,
friendfund